const prompt = require("prompt-sync")();
// storing nama of the person
let name = prompt(" Enter Name ");
// printing the message 
console.log(`HI ${name}, would u like to play a game with Me`);
